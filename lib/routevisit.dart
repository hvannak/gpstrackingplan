import 'package:flutter/material.dart';

class Routevisit extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Route Visit'),
      ),
    );
  }
}